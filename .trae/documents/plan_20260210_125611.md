Vou corrigir o cálculo do valor total no card do mensalista e adicionar a indicação visual de serviços extras.

### Plano de Implementação:

1.  **Atualizar Cálculo (`MonthlyClientCard` em `App.tsx`):**
    *   Modificar a função `calculateTotalInvoiceValue` dentro do componente `MonthlyClientCard`.
    *   Garantir que ela some **todos** os tipos de serviços extras disponíveis no objeto `extra_services` (não apenas os de estética, mas também pernoite, dias extras, adestrador, etc.), conforme o exemplo dado (pernoite + dias extras).
    *   A lógica deve iterar sobre todas as chaves de `extra_services` e somar `value` (ou `quantity * value` se aplicável).

2.  **Adicionar Indicador Visual (Label):**
    *   No mesmo componente `MonthlyClientCard`, verificar se o valor total calculado é maior que o preço base (`client.price`).
    *   Se for maior, renderizar um `span` ou `Badge` ao lado do preço com o texto "(+ Extras)" ou similar, estilizado para destaque (ex: cor roxa/indigo conforme padrão visual).

3.  **Verificação:**
    *   Confirmar que o exemplo do usuário (Base 240 + Pernoite 50 + Dias Extras 200 = 490) seja calculado corretamente.

**Arquivos afetados:**
*   `App.tsx` (Componente `MonthlyClientCard`)