I will implement the requested enhancements for the Appointment Card:

1.  **Update Data Structures**:
    *   Modify `types.ts` to add `pet_photo_url` and `recurrence_type` fields to the `AdminAppointment` interface.
    *   Update `App.tsx` to fetch these fields from the `monthly_clients` table using a Supabase join query (`select('*, monthly_clients(...)')`) and map them in the `normalize` function.

2.  **Enhance `AppointmentCard.tsx`**:
    *   **Pet Photo**: Display the pet's photo (from `pet_photo_url`) for monthly clients, replacing the default emoji.
    *   **Recurrence Label**: For monthly clients, display the recurrence type ("Semanal", "Quinzenal", "Mensal") in the header badge, matching the Monthly Client card style.
    *   **Extras Badges**: Replace the generic "+ Extras" text with individual badges for each active extra service (e.g., "Pernoite", "Só Banho"), derived from the `extra_services` data.

I will verify these changes by ensuring the data flows correctly from the fetch to the component and the UI reflects the specific requirements.
