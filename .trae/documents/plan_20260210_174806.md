Para corrigir a responsividade do rodapé do modal de serviços extras, ajustarei a estrutura do flex container e o tamanho dos botões.

**Plano de Ação:**
1.  No arquivo `ExtraServicesModal.tsx`, no rodapé (aprox. linha 211):
    *   Alterar o container principal para permitir quebra de linha em telas menores (`flex-wrap`).
    *   Ajustar o texto "Base: ... + Extras: ..." para incluir "Serviço" em vez de "Base".
    *   Modificar a estrutura para que o total fique em uma linha e os botões em outra em telas pequenas, ou lado a lado em telas maiores.
    *   Reduzir o padding horizontal (`px`) dos botões para economizar espaço ou usar `flex-1` para distribuição uniforme.

**Alterações Específicas:**
*   Container: `flex flex-col sm:flex-row justify-between items-center p-4 sm:p-6 gap-4`
*   Texto Base: `Serviço: R$ ... + Extras: R$ ...`
*   Botões: Ajustar `px` ou usar classes responsivas para largura total em mobile.
