O usuário solicitou ajustes visuais e a correção de um erro no `StatisticsDashboardModal.tsx`.

### 1. Correção do Erro `ReferenceError: distributionData is not defined`
- **Causa**: No `useMemo`, a variável `distributionData` foi removida ou renomeada em uma edição anterior, mas ainda está sendo referenciada no retorno do objeto (propriedade `serviceDistribution`).
- **Solução**: Restaurar a lógica de cálculo de `distributionData` dentro do `useMemo` antes do retorno, garantindo que `serviceDistribution` receba um valor válido.

### 2. Remoção da `div` (Detalhamento de Serviços)
- **Ação**: Remover a tabela de "Detalhamento de Serviços" da `OperationalSection`.
- **Localização**: Dentro de `OperationalSection`, remover o bloco `<div className="bg-white p-6 rounded-2xl shadow-sm border border-gray-100">...Detalhamento de Serviços...</div>`.

### 3. Renomeação do `h3` "Distribuição"
- **Ação**: Alterar o texto "Distribuição" para "Distribuição por Serviços".
- **Localização**: Dentro de `OperationalSection`, no título do gráfico de rosca (`SimpleDonutChart`).

### 4. Reposicionamento do Título "Financeiro"
- **Ação**: Mover o título "Receita Diária (Semana Atual)" (que é um `h3` dentro de um card) para fora ou adicionar um título de seção "Financeiro" acima do card, conforme solicitado ("O titulo 'Financeiro' deve ficar acima do card de Receita Diária").
- **Observação**: Na verdade, o usuário pediu "O titulo 'Financeiro' deve ficar acima do card de Receita Diária". Atualmente, "Receita Diária" está na `OverviewSection`. O título "Financeiro" geralmente é um cabeçalho de seção (`h3` com classe `text-lg font-bold...`). Vou verificar se há um título "Financeiro" solto ou se devo adicionar um título de seção explícito acima do grid que contém o gráfico de receita diária na `OverviewSection` (embora visualmente possa ficar redundante se já houver um título de aba, seguirei a instrução de hierarquia visual).
- **Ajuste Específico**: Na visualização mobile (onde as seções são empilhadas), já existe `<h3 ...>Financeiro</h3>`. Na visualização desktop (`OverviewSection`), o gráfico de receita diária está "solto". Vou garantir que o título da seção ou do card esteja alinhado com a expectativa. Se o usuário se refere ao texto "Financeiro" que aparece na versão mobile, vou ajustar a ordem. Se for na desktop, vou adicionar um cabeçalho se necessário.
    - *Revisão do pedido*: "O titulo 'Financeiro' deve ficar acima do card de Receita Diária". O card de receita diária tem um título interno "Receita Diária...". O usuário provavelmente quer que a seção que contém esse card tenha um título "Financeiro" acima dela, ou que o título interno do card mude. Pelo contexto de "Sessão section", parece ser na estrutura da página.
    - *Decisão*: Vou adicionar/mover o título de seção apropriado.

### Resumo das Alterações no Arquivo `src/components/StatisticsDashboardModal.tsx`:
1.  **Restaurar `distributionData`**: Recalcular a distribuição de serviços (Banho, Banho & Tosa, Pet Móvel) dentro do `useMemo`.
2.  **Editar `OperationalSection`**:
    *   Renomear título para "Distribuição por Serviços".
    *   Remover a tabela de detalhamento.
3.  **Ajustar `OverviewSection`**:
    *   Verificar a posição do título "Financeiro" ou "Receita Diária". O usuário mencionou "O titulo 'Financeiro' deve ficar acima do card de Receita Diária". Vou inserir um `<h3 className="text-lg font-bold text-gray-800 mb-4">Financeiro</h3>` antes do grid que contém o gráfico de receita diária na `OverviewSection`, para criar essa separação visual clara.
