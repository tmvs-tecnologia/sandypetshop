I will modify `src/components/StatisticsDashboardModal.tsx` to implement the requested "Mensalistas" session in the mobile view, replacing the current placeholder.

**Plan details:**

1.  **Update Data Processing (`metrics`)**:
    *   Modify the `monthlyStats` object creation (inside `useMemo`) to include the `monthlyPrice` (base monthly fee) from the `monthly_clients` table. This allows displaying the "total value" (subscription price) in the list as requested.

2.  **Implement "Mensalistas" Section (Mobile View)**:
    *   Locate the `md:hidden` block where the "Módulo de Mensalistas" placeholder currently exists (lines ~776).
    *   Replace the placeholder with a real content section containing:
        *   **Charts**:
            *   **"Top 5: Mais Serviços Concluídos"**: Reuse the existing `HorizontalBarChart` component with `metrics.monthlyStats.topServices`. This chart is already animated.
            *   **"Top 5: Maior Investimento"**: Reuse the existing `HorizontalBarChart` component with `metrics.monthlyStats.topRevenue`.
        *   **List of Mensalistas**:
            *   Create a list displaying the monthly subscribers.
            *   Each item will show the **Pet's Photo**, **Name**, and the **Subscription Value**.
            *   **Crucial**: The value will be displayed using the specific styling requested: `<div class="font-outfit font-bold text-lg text-gray-900 whitespace-nowrap">R$ ...</div>`.

3.  **Verification**:
    *   I will verify that the new section appears correctly on the mobile view logic.
    *   I will ensure the charts render with the correct data.
    *   I will check that the price tag uses the requested classes.
