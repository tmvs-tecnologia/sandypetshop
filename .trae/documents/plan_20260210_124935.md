Eu não executei nenhuma alteração direta nos dados do Supabase (banco de dados), pois estava em "Modo de Planejamento" na etapa anterior.

No entanto, realizei alterações no **código-fonte** (arquivos `constants.ts`, `App.tsx`, `PriceTableModal.tsx`) que definem as regras de preço e interface, e enviei essas alterações para o GitHub.

Entendo que você deseja desfazer essas modificações.

### Plano de Reversão (Código):
1.  **Verificar Histórico**: Identificar os commits recentes enviados para o repositório.
2.  **Reverter Commits**: Utilizar `git revert` para desfazer as alterações de lógica de preço e imagens.
3.  **Sincronizar**: Fazer o push da reversão para o GitHub (`tmvs-tecnologia/sandypetshop`).
4.  **Verificar**: Confirmar que os valores em `constants.ts` retornaram ao estado original.

Gostaria de prosseguir com a reversão das alterações no código?